<% flash.each do |name, msg| %>
  <%= name %>: <%= msg %>
<% end %>
</br></br>

<% @feeds.each do |feed| %>
	<%= feed.inspect %>
	<%= link_to 'Edit feed', edit_feed_path(feed) %>
	<%= link_to 'Delete feed', feed, :method => :delete %>
	</br>
<% end %>

<%= link_to 'Add new feed', new_feed_path %>